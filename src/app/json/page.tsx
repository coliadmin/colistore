export default async function JsonPage() {
  // const domain = getDomain();
  // const company = await getCompany(domain);

  return <pre>{/* <code>{JSON.stringify(company, null, 2)}</code> */}</pre>;
}
